# تشغيل بيتنا POS على أكثر من جهاز

يعمل جهاز واحد كسيرفر مركزي، وتظل قاعدة SQLite على هذا الجهاز فقط. تتصل بقية أجهزة
الكاشير والمطبخ والتجميع بالسيرفر عبر API، وتصلها التحديثات الفورية عبر WebSocket.

## الجهاز الرئيسي

1. افتح التطبيق على الجهاز الذي سيبقى قيد التشغيل.
2. افتح **الإعدادات ← السيرفر والشبكة**.
3. اترك العنوان `http://127.0.0.1:4312`.
4. انسخ عنوان الشبكة الذي يظهر في بطاقة جهاز السيرفر.
5. وافق على وصول التطبيق إلى الشبكات الخاصة إذا طلب Windows Firewall ذلك.

عند أول تشغيل ينقل التطبيق البيانات الموجودة في `beitna.db` إلى مخزن الحالة المركزي
داخل الملف نفسه، بدون حذف الجداول أو البيانات القديمة.

## أجهزة الكاشير والمطبخ الإضافية

1. يجب أن يكون الجهاز على نفس شبكة جهاز السيرفر.
2. افتح **الإعدادات ← السيرفر والشبكة**.
3. اكتب عنوان جهاز السيرفر، مثل `http://192.168.1.10:4312`.
4. اضغط **اختبار الاتصال** ثم **حفظ وإعادة الاتصال**.

بعد الحفظ لا يشغّل الجهاز الإضافي سيرفرًا محليًا في مرات التشغيل التالية، بل يتصل
مباشرة بالجهاز الرئيسي.

## ملاحظات تشغيل

- المنفذ الافتراضي هو `4312`.
- لا يحتاج النظام إلى إنترنت؛ يكفي وجود شبكة LAN أو Wi-Fi محلية.
- لا تضع ملف SQLite في مجلد شبكي مشترك، ولا تفتحه من الأجهزة الإضافية.
- شغّل جهاز السيرفر قبل أجهزة الكاشير والمطبخ.
- احتفظ بنسخة احتياطية يومية من شاشة النسخ الاحتياطي.
